# Copy this file to .env for a simple local installation.
# The JSON store is intended for local testing and small single-instance sites.
STORAGE=json
DATA_DIR="./data"
HOST="0.0.0.0"
PORT="3000"
TRUST_PROXY="0"

# Leave these empty to disable /admin, or set both to enable it.
ADMIN_PASSWORD=""
ADMIN_SESSION_SECRET=""

# To use MongoDB instead, set STORAGE=mongodb and provide one connection URL.
# STORAGE=mongodb
# MONGO_URL="mongodb://user:password@host:27017/chikochan"
# MONGODB_URI="mongodb://user:password@host:27017/chikochan"
# MONGO_DB_NAME="chikochan"
